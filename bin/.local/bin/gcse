#!/usr/bin/env bash

exam=$(read -p "What is the exam?")
echo "What paper is it?"
paper=$(read -p "What paper is it?")

echo $exam

date=$(date +%Y-%m-%d)

pushd ~/GCSE/
touch "$exam-$pape-yoni-kosiner-$date-91943.md"
popd

#!/usr/bin/env bash

item="$(echo $HOME)/.dotfiles/anime/$(ls ~/.dotfiles/anime | dmenu -p "Backgrounds" -l 30)"

if [[ $item != "$HOME/.dotfiles/anime/" ]]; then
    feh --bg-fill $item
    # dconf write /org/mate/desktop/background/picture-filename "'$item'"
fi

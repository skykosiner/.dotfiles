#!/usr/bin/env bash

isCharging=$(acpi | awk '{print $3}' | sed 's/,//')
battLevel=$(acpi | awk '{print $4}' | sed 's/,//')

if [[ $isCharging == "Discharging" ]];
then
    echo "🔋"$battLevel
else
    echo "🔌"$battLevel
fi


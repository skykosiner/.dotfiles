#!/usr/bin/env bash

case $1 in
    "--full")
        brightnessctl set 100%
        ;;
    "--dec")
        brightnessctl set +5%
        ;;
    "--inc")
        brightnessctl set 5%-
        ;;
    *) echo "Unavailable command... $curr"
esac

#!/usr/bin/env bash

while [ "$#" -gt 0 ]; do
    curr=$1
    shift

    case "$curr" in
   "-inc-1")
       airconNumber=$(/home/yoni/.local/bin/aircon status | awk '{print $1}' | sed s/{// | awk -F "." '{print $1}')
       airconNumber=$(( $airconNumber + 1 ))

       /home/yoni/.local/bin/aircon $airconNumber
       ;;
   "-dec-1")
       airconNumber=$(/home/yoni/.local/bin/aircon status | awk '{print $1}' | sed s/{// | awk -F "." '{print $1}')
       airconNumber=$(( $airconNumber - 1 ))

       /home/yoni/.local/bin/aircon $airconNumber
       ;;
    *) echo "Unavailable command... $curr"
    esac
done
